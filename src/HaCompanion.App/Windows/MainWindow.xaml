<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HaCompanion.App.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Repair banner: only for terminal, user-fixable failures (token/certificate). -->
        <InfoBar x:Name="RepairBar" Grid.Row="0" Severity="Warning" IsOpen="False"
                 Margin="8,8,8,0" CloseButtonClick="RepairBar_CloseClick">
            <InfoBar.ActionButton>
                <Button Content="{Binding [Repair_Open], Source={StaticResource Loc}}"
                        Click="RepairOpenSettings_Click" />
            </InfoBar.ActionButton>
        </InfoBar>

        <NavigationView x:Name="Nav"
                        Grid.Row="1"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        Loaded="Nav_Loaded"
                        SelectionChanged="Nav_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="{Binding [Nav_Start], Source={StaticResource Loc}}" Tag="dashboard">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Dashboards], Source={StaticResource Loc}}" Tag="hadashboards">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE774;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Shortcuts], Source={StaticResource Loc}}" Tag="shortcuts">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE765;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Automations], Source={StaticResource Loc}}" Tag="automations">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE945;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_MyPc], Source={StaticResource Loc}}" Tag="mypc">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE770;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Settings], Source={StaticResource Loc}}" Tag="settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <tb:TaskbarIcon x:Name="Tray"
                        ToolTipText="HA Companion"
                        ContextMenuMode="SecondWindow"
                        IconSource="ms-appx:///Assets/tray.ico">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="{Binding [Tray_Open], Source={StaticResource Loc}}" Click="Tray_Open">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE7C4;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="{Binding [Tray_Panel], Source={StaticResource Loc}}" Click="Tray_QuickPanel">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE8A0;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="{Binding [Tray_Reconnect], Source={StaticResource Loc}}" Click="Tray_Reconnect">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE895;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="{Binding [Tray_Exit], Source={StaticResource Loc}}" Click="Tray_Exit">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE7E8;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</Window>
